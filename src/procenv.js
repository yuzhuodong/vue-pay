
const conf = window._ekconfig
const baseUrl = conf.baseUrl
const commUrl = conf.commUrl
const authUrl = conf.authUrl
const demo = conf.demo

export default {
  baseUrl,
  commUrl,
  authUrl,
  demo
}
