// import * as types from './mutationTypes'
// import * as lstore from '@/utils/storage'

export default {
  // saveMenus (state, data) {
    // JSON.stringify(data)
    // lstore.setLStore(types.MENUS, JSON.stringify(data))
    // state.menus = data
 // }
}
